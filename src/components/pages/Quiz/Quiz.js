import React, { Fragment } from 'react'
import { Navbar, QNA } from '../../../components'

export const Quiz = () => {
    return (
        <Fragment>
            <Navbar />
            <QNA />
        </Fragment>
    )
}
